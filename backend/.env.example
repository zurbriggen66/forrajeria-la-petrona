SECRET_KEY=cambiar-esta-clave-en-.env-local
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# SQLite por defecto si se omite. Para Postgres:
# DATABASE_URL=postgres://usuario:password@localhost:5432/kubopos

CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Fiscal AFIP/ARCA (Fase 7 — mockeado por ahora, no cargar certificados reales acá)
# AFIP_CERT_PATH=
# AFIP_KEY_PATH=
# AFIP_CUIT=

# Bot de WhatsApp (whatsapp-bot/, vinculado por QR) — vacío = notificaciones desactivadas
# WHATSAPP_BOT_URL=http://localhost:3001
# WHATSAPP_BOT_API_KEY=

# Asistente con IA (Claude). Vacío = el asistente queda apagado.
# La key se saca de console.anthropic.com y vive SÓLO acá, nunca en el frontend.
# ANTHROPIC_API_KEY=
# ASISTENTE_MODELO=claude-opus-5
# ASISTENTE_EFFORT=medium
