SECRET_KEY=cambiar-esta-clave-en-.env-local
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# SQLite por defecto si se omite. Para Postgres:
# DATABASE_URL=postgres://usuario:password@localhost:5432/kubopos

CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Fiscal AFIP/ARCA (Fase 7 — mockeado por ahora, no cargar certificados reales acá)
# AFIP_CERT_PATH=
# AFIP_KEY_PATH=
# AFIP_CUIT=

# WhatsApp (fuera de alcance por ahora — proveedor sin definir)
# WHATSAPP_PROVIDER=
# WHATSAPP_API_TOKEN=
